version: "3"
services:
  app:
    container_name: {{ cookiecutter.project_slug }}
    build:
      context: .
      args:
        NODE_VERSION: "{{ cookiecutter.node_version }}"
    ports:
      - 3000:3000
